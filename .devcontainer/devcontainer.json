{
    "name": "Basic Ubuntu with git and vim",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".",
        "args": {}
    },
    "customizations":{
        "vscode": {
            "extensions": [],
            "settings": {}
        }
    },
    "forwardPorts": [],
    "postCreateCommand": "",
    "postAttachCommand": [
        "rm ~/.gitconfig",
        "git config --global --add safe.directory /workspaces/git-basics-example",
    ],
    "remoteUser": "root"
}
